# === Required ===
BACKUP_DESTINATION="$HOME/backups"       # Where .tar.gz files go

# === Exclusions (comma-separated) ===
# These become multiple --exclude patterns for tar
EXCLUDE_PATTERNS=".git,node_modules,.cache,.venv,*.tmp"

# === Retention policy ===
DAILY_KEEP=7
WEEKLY_KEEP=4
MONTHLY_KEEP=3

# === Optional email (not used by default; add your own mail send) ===
NOTIFY_EMAIL=""



